{
  "extends": "@react-native/typescript-config",
  "compilerOptions": {
    "types": ["jest"]
  },
  "include": ["**/*.ts", "**/*.tsx"],
  // `landing/` is a separate Vite web project with its own tsconfig (DOM libs).
  "exclude": ["**/node_modules", "**/Pods", "landing"]
}
